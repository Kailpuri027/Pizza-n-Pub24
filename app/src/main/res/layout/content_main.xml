<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context="capstone.conestoga.pizzanpub24.MainActivity"
    tools:showIn="@layout/app_bar_main">

    <ImageView
        android:id="@+id/header"
        android:layout_width="match_parent"
        android:layout_height="90dp"
        android:layout_alignParentTop="true"
        android:scaleType="fitXY"
        android:src="@drawable/header" />

    <ImageView
        android:id="@+id/pizza"
        android:layout_width="match_parent"
        android:layout_height="200dp"
        android:layout_below="@+id/header"
        android:layout_centerHorizontal="true"
        android:scaleType="fitXY"
        android:src="@drawable/pizzabg" />

    <TextView
        android:id="@+id/textView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/pizza"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="15dp"
        android:text="Select order type" />

    <ImageButton
        android:id="@+id/deliveryButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignTop="@+id/textView"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="20dp"
        android:background="@drawable/rect"
        android:onClick="delivery"
        android:scaleType="fitXY"
        android:src="@drawable/deliverybutton" />

    <ImageButton
        android:id="@+id/carryoutButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/deliveryButton"
        android:layout_below="@+id/deliveryButton"
        android:layout_marginTop="10dp"
        android:background="@drawable/rect"
        android:onClick="delivery"
        android:scaleType="fitXY"
        android:src="@drawable/carryoutbutton" />

</RelativeLayout>
